{
  "compilerOptions": {
    // Настройки для современного TSX/React-проекта
    "target": "ES2020",
    "useDefineForClassFields": true,
    
    // FIX TS6046: Упрощение и использование проверенных значений для lib
    "lib": ["ES2020", "DOM"], 
    
    "module": "ESNext",
    "skipLibCheck": true,

    // Настройки для сборщика (Vite)
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true, 
    "jsx": "react-jsx", // Обязательно для React

    // Строгие правила
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  // Указываем, что нужно включить все файлы из папки src
  "include": ["src/**/*.ts", "src/**/*.tsx"]
  
  // FIX TS6306 & TS6310: Удаляем секцию references, поскольку она требует 
  // наличия и правильной настройки tsconfig.node.json, что вызывает ошибки.
  // "references": [{ "path": "./tsconfig.node.json" }] 
}